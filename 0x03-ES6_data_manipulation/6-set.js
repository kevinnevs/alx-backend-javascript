/**
 * Creates a set from an array.
 * @param {Array} array - The source of array.
 * @author Kevin Njuguna <https://github.com/kevinnevs>
 * @returns {Set}
 */
export default function setFromArray(array) {
  return new Set(array);
}
